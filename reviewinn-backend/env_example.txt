# Environment Configuration for Enhanced Authentication System

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost/reviewinn
TEST_DATABASE_URL=postgresql://username:password@localhost/reviewinn_test

# JWT Configuration
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30

# Email Configuration (SMTP)
# Gmail Example:
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true

# Email Settings
FROM_EMAIL=noreply@reviewinn.com
FROM_NAME=ReviewInn

# Email Security Settings
EMAIL_VERIFICATION_EXPIRE_MINUTES=15
PASSWORD_RESET_EXPIRE_MINUTES=15
RESEND_COOLDOWN_MINUTES=2
MAX_EMAIL_ATTEMPTS=5

# Email Rate Limiting
EMAIL_RATE_LIMIT_WINDOW=60
MAX_EMAIL_REQUESTS=3

# Email Feature Flags
ENABLE_EMAIL_SENDING=true
EMAIL_DEBUG_MODE=false

# Frontend Configuration
FRONTEND_URL=http://localhost:5173
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]

# Redis Configuration (for production)
REDIS_URL=redis://localhost:6379/0

# Security Configuration
RATE_LIMIT_REQUESTS_PER_MINUTE=100
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCKOUT_DURATION_MINUTES=15

# Application Settings
DEBUG=false
ENVIRONMENT=development
LOG_LEVEL=INFO

# API Configuration
API_V1_PREFIX=/api/v1
MAX_UPLOAD_SIZE_MB=10